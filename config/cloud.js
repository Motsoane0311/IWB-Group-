// cloud.js

const cloudConfig = {
    provider: 'aws',
    bucketName: '',
    accessKeyId: '',
    secretAccessKey: '',
    region: 'af-south-1', // Cape Town (optional to leave for now)
};

module.exports = cloudConfig;
